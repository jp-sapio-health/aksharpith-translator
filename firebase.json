{
  "functions": {
    "source": "functions",
    "predeploy": [
      "npm --prefix functions run prebuild",
      "npm --prefix functions run build"
    ]
  },
  "firestore": {
    "rules": "firestore.rules"
  },
  "storage": {
    "rules": "storage.rules"
  }
}
